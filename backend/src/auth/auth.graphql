enum AuthType {
  EMAIL
  OFFICE
}
type User {
  id: ID!
  fullName: String!
  email: String!
  created_at: Date!
  updated_at: Date
}
type Session {
  token: String!
}
union AuthenticatedUser = User | Session


type Query {
  session: AuthenticatedUser
}

type Mutation {
  loginOffice(token: String!): AuthenticatedUser
  loginEmail(email: String!, password: String!): AuthenticatedUser
  createUserEmail(email: String!, password: String!): User
  logout: Boolean
}