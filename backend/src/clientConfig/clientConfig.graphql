type Validation {
    taskSubjectMaxLength: Int!
    taskIssueMaxLength: Int!
    subtaskMaxLength: Int!
}

type ClientConfig {
    validation: Validation!
    preferencies: Preferencies!
}

type Query {
    clientConfig: ClientConfig!
}

type Preferencies {
    language: String!
    theme: String!
}
